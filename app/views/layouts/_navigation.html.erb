<ul class="nav">
	<li>
		<%= link_to "Postit!", root_path, :class => 'brand' %>
	</li>
	<% if logged_in? %>
		<li>
		 	<div class='btn btn-success'>Welcome, <%= current_user.username %>!</div>
		</li>
	<% end %>
	<li>
		<%= link_to 'All Posts', posts_path %>
	</li>

	<% if logged_in? %>
		<li>
			<%= link_to 'New Post', new_post_path %>
		</li>
		<li>
			<%= link_to 'New Category', new_category_path %>
		</li>
		<li>
			<%= link_to 'Logout', logout_path %>
		</li>
	<% else %>
		<li>
		 <%= link_to 'Login', login_path %>
	  </li>
		<li>
			<%= link_to 'Register', register_path %>
		</li>
	<% end %>
	<li>
		<div class="btn-group">
  		<a class="btn dropdown-toggle" data-toggle="dropdown" href="#">Categories <span class="caret"></span></a>
  		<ul class="dropdown-menu">
  			<% categories.each do |category| %>
  				<li><%= link_to category.name, category %></li>
  			<% end %>
		    <li class="divider"></li>
		    <li><a href="categories/new"><i class="icon-plus"></i> New Cateogory</a></li>
		  </ul>
		</div>
	</li>
</ul>
